apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: chargeops-dev-frontend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Dhanigasree/ChargeOps.git
    targetRevision: develop
    path: argocd/dev/frontend
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
