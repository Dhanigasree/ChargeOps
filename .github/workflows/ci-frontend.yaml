# File: .github/workflows/ci-frontend.yaml
name: CI - Frontend (React)

on:
  push:
    branches: [dev, main]
    paths:
      - "ev-frontend/**"
  pull_request:
    branches: [dev, main]
    paths:
      - "ev-frontend/**"

jobs:
  ci:
    name: Run CI template
    uses: ./.github/workflows/ci-frontend-template.yaml
    with:
      service: frontend
      working_directory: apps/frontend
      node_version: "20"
      test_command: "npm run test -- --coverage --watchAll=false"
      sonar_project_base_dir: apps/frontend
      image_name: ${{ secrets.DOCKERHUB_USERNAME }}/frontend
      pr_number: ${{ github.event.pull_request.number }}
      head_ref: ${{ github.head_ref }}
      actor: ${{ github.actor }}
      pr_url: ${{ github.event.pull_request.html_url }}
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
      MAIL_USERNAME: ${{ secrets.MAIL_USERNAME }}
      MAIL_PASSWORD: ${{ secrets.MAIL_PASSWORD }}
      NOTIFY_EMAIL: ${{ secrets.NOTIFY_EMAIL }}
