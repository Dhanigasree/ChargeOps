apiVersion: v1
kind: Service
metadata:
  name: admin-service-preview
  namespace: prod
spec:
  type: ClusterIP
  selector:
    app: admin-service
    role: preview
  ports:
    - name: http
      port: 3000
      targetPort: 3000
