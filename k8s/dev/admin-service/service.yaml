apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: dev
  labels:
    app: admin-service
spec:
  type: ClusterIP
  selector:
    app: admin-service
  ports:
    - name: http
      port: 8007
      targetPort: http
