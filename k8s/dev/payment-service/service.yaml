apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: dev
  labels:
    app: payment-service
spec:
  type: ClusterIP
  selector:
    app: payment-service
  ports:
    - name: http
      port: 8005
      targetPort: http
